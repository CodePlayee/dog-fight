{
  "name": "ace-of-dawn",
  "version": "1.0.0",
  "description": "Ace of Dawn — a WW2 dogfight web game built with Three.js",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:prod": "npm run build && playwright test -c playwright.production.config.js",
    "check": "npm test && npm run test:prod"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "pngjs": "^7.0.0",
    "vite": "^8.1.4"
  }
}
